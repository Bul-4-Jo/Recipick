import styled from 'styled-components';

export const ProfileImgWrapper = styled.div`
  text-align: center;
  margin: 30px auto;
  img {
    position: relative;
  }
  button {
    position: absolute;
    bottom: 0;
    right: 0;
  }
`;
